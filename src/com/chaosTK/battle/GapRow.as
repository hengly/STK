package com.chaosTK.battle
{
    internal class GapRow
    {
        public var idx : int;
        public var step : int;

        public function GapRow(idx : int, step : int)
        {
            this.idx = idx;
            this.step = step;
        }
    }
}
